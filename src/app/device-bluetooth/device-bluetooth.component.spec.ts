/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DeviceBluetoothComponent } from './device-bluetooth.component';

describe('Component: DeviceBluetooth', () => {
  it('should create an instance', () => {
    let component = new DeviceBluetoothComponent();
    expect(component).toBeTruthy();
  });
});
